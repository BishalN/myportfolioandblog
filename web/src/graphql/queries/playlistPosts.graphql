query PlaylistPosts($slug: String!) {
  playlists(where: { slug: $slug }) {
    id
    title
    description
    posts {
      id
      updated_at
      title
      topics
      slug
      description
    }
  }
}
